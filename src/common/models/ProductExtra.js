export default class {
  constructor({
    id,
    // productId,
    avatarUrl,
    price,
    volume,
    weight,
  }) {
    this.id = id
    this.avatarUrl = avatarUrl
    this.price = price
    this.volume = volume
    this.weight = weight
  }
}
