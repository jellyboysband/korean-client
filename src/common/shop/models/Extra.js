export default class Extra {
  constructor({
    id,
    productId,
    avatarUrl,
    price,
    volume,
    weight,
  }) {
    this.id = id
    this.productId = productId
    this.avatarUrl = avatarUrl
    this.price = price
    this.volume = volume
    this.weight = weight
  }
}
